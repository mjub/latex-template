\usepackage{
  enumitem,
  fancyhdr,
  % showframe,
  titlesec,
  titletoc,
  xcolor,
}

\setlist[enumerate]{label=(\roman*)}

% -- Geometry.
\usepackage[
  a4paper,
  headsep=.5cm,
  includeheadfoot,
  % Must be set before inner.
  hmargin=4cm,
  vmargin=3cm,
  % inner=3.7cm,
  nofoot,
]{geometry}

% -- Fonts.
\usepackage[
  activate={true,nocompatibility},
  babel,
  tracking,
]{microtype}

\usepackage[
  largesc,
  smallerops,
  theoremfont,
  trueslanted,
  varbb,
  varg,
  varvw,
]{newtx}
